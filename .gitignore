# Env files (obsahujú heslá/secrets, nikdy necommitovať)
.env
*.env
!.env.example

# client.ovpn: verified 2026-08-17 to be byte-identical (SHA-256) to STU's own public download
# (stuba.sk/buxus/docs/stu/pracoviska/cvt/navody/client.ovpn) - not a secret, tracked normally.
# VPN credentials now come from infra/.env (VPN_USERNAME/VPN_PASSWORD), no more auth.txt file.

# OS
.DS_Store

# Claude Code lokálne nastavenia
.claude/settings.local.json
CLAUDE.md

# IDE (repo-wide, projektové .gitignore v backend/frontend riešia zvyšok)
.idea/
.vscode/
*.iml

# Frontend (kým nepribudne frontend/.gitignore)
node_modules/
dist/

# one-off migration output (contains real PII - emails, discord ids)
migration.sql
